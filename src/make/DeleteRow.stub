<?php

namespace {%namespace%};

use Meioa\Tools\action\DeleteRow;


class {%className%}
{
    public function run(){
        $data = request()->post();
        return (new DeleteRow('{%table%}','{%tablePk%}'))->run($data);
    }
}
