<?php

namespace {%namespace%};
use Meioa\Tools\action\BaseAddRow;


class {%className%}
{
    public function run(){
        $data = request()->post();
        return (new BaseAddRow('{%table%}','{%tablePk%}'))->run($data);
    }
}
