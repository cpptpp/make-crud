<?php

namespace {%namespace%};

use Meioa\Tools\action\PageQuery;


class {%className%}
{
    public function run(){
        $data = request()->post();
        return (new PageQuery('{%table%}','{%tablePk%}'))->run($data);
    }
}
